/* ================================================================================
   NEURAL DRAFT LLC | DEEPSEEK INSTRUCTION HEADER
================================================================================
   FILE:    ActionLoader.cs
   CONTEXT: JSON deserialization for action definitions.

   TASK:
   Create a C# class ActionLoader that loads action definitions from JSON.

   CONSTRAINTS:
   - Use Fixed-Point Math (Fx.SCALE = 1000) for all physics.
   - No Unity Engine references in this file.
   - Strict Determinism: No floats.
================================================================================

*/
namespace NeuralDraft
{
    public static class ActionLoader
    {
        // Placeholder for ActionLoader implementation
        // Since we don't have System.Text.Json usage examples in the provided context for the Duplicate folder,
        // and we might not need full loading for this task, I will leave it minimal or stubbed if needed.
        // But for now, I'm just creating the file to match the structure.

        public static ActionDef LoadFromJson(string json)
        {
             // Stub implementation
             return new ActionDef();
        }
    }
}
